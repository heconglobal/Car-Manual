{
  "name": "fiero-3d-manual",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5185 --strictPort",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 5185 --strictPort",
    "test": "playwright test"
  },
  "dependencies": { "three": "^0.180.0" },
  "devDependencies": { "vite": "^8.0.12", "@playwright/test": "^1.60.0" }
}
